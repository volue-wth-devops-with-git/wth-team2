name: jobs

on: workflow_dispatch

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: echo "step1"

      - name: step2
        run: echo "step2"



  second_jon:
    needs: first_job
    runs-on: ubuntu-latest
    steps:
    - name: Ship it 
      uses: mscoutermarsh/cowsays-action@master
      with:
        text: 'ship ship ship!'
        color: 'white'
